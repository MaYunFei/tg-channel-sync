# 更新说明

## v0.3.7

本版本重点完成了同步模块的目录重构、JSON 导入与下载重传的公共发送层抽取，并补齐了 JSON 导入图片/视频指纹重置能力。同时修复了 Windows 便携包构建脚本在 PowerShell 与文件占用场景下的不稳定问题。

### 主要更新

- 重构 `sync_worker` 目录结构，按职责拆分为：
  - `runtime`
  - `core`
  - `media`
  - `senders`
  - `clone`
  - `json_import`
- 删除根层兼容壳文件，统一通过正式模块目录组织同步逻辑。
- 保留 `sync_worker.__init__` 作为稳定导出层，对外入口不变。

### JSON 导入

- `JSON 导入` 现已支持 `重置图片/视频指纹`。
- 指纹重置采用“复制到临时目录后处理”的方式，不修改原始导出文件。
- JSON 单条媒体与媒体组统一复用公共媒体准备逻辑。
- JSON 发送身份选择、Bot 失败回退辅助账号、媒体组合并窗口等能力继续保留。

### 下载重传与发送层

- 抽出统一发送目标选择层，复用 `bot / 辅助账号` 选择、回退与冷却逻辑。
- 抽出统一单文件发送入口，收敛文本、图片、视频、文件、贴纸等发送分支。
- 抽出统一媒体组构建逻辑，统一处理 caption、thumbnail / thumb、streaming 参数。
- clone 与 JSON 两条链路改为复用同一套发送基础设施。

### 打包与构建

- 修复 `build-portable.ps1` 在 `powershell.exe` 下的解析问题。
- 修复压缩阶段因 DLL 占用导致的打包失败，新增自动重试。
- 调整临时打包目录策略，避免失败重跑时被旧残留目录卡死。
- 构建结束时对清理失败改为告警，不再误判整轮打包失败。

### 测试与稳定性

- 更新测试导入路径，移除对旧兼容壳文件的依赖。
- 新增发送目标选择相关测试。
- 全量测试已通过。

## v0.3.6

本版本重点完善了 `JSON 导入`、`下载重传`、日志展示以及上传回退策略，适合作为 `v0.3.x` 分支的预发布验证版本。

### 主要更新

- `JSON 导入` 新增发送身份选择，支持 `机器人` 与 `辅助账号` 两种发送方式。
- `JSON 导入` 新增 `Bot 发送失败时回退辅助账号继续发送` 开关，复用下载重传现有的上传目标选择逻辑。
- `JSON 导入` 支持源频道用户名填写 `@username` 或 `https://t.me/username`，用于消息链接改写。
- `JSON 导入` 增加媒体组合并窗口配置，可按秒数合并连续图片、视频和文件消息。
- `JSON 导入` 完善富文本实体还原，补齐粗体、斜体、下划线、删除线、剧透、代码、预格式化、引用和链接样式。
- `JSON 导入` 修复 `t.me` 消息链接改写异常、视频类型识别错误、动态贴纸回退策略等问题。

### 下载重传改进

- 上传阶段支持更完整的 `Bot -> 辅助账号` 回退链路。
- 针对 `Request Entity Too Large`、`retry after N`、`FLOOD_WAIT_X` 等场景补齐自动等待、轮换和回退逻辑。
- 针对不允许转发的频道，给出明确提示：请改用下载重传。
- 为媒体组视频和单条视频补齐缩略图透传，降低黑封面问题出现概率。
- 修复并发下载同名文件时的临时文件冲突问题。

### 日志与前端

- 日志面板改为从上到下、由旧到新展示。
- 自动滚动改为条件跟随：手动上滑后不会被新日志强行拉回底部。
- 黑色日志背景下重新分层上色，区分时间、级别、动作与正文。
- 上传状态改为双行展示，并支持显示百分比、已传大小、总大小和速度。
- 任务正常完成、停止或异常结束时，系统日志会写入明确结论。

### 稳定性与测试

- 修复数据库连接在不同事件循环切换时的处理问题，减少测试阶段和接口调用中的跨 loop 异常。
- 新增并更新 JSON 导入、链接改写、媒体组处理、上传回退和数据库清理相关测试。
- 全量测试已通过。

### 说明

- 本次发布为 `pre-release`，用于在真实场景下继续验证 JSON 导入与下载重传链路。
- 未包含构建产物，本次仅同步源码与版本标记。
