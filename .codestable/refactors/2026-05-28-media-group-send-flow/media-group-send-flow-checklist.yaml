refactor: 2026-05-28-media-group-send-flow
status: draft
steps:
  - id: 1
    title: 提取 JSON 媒体组准备阶段
    method: M-L2-01 Extract Function; M-L2-07 Introduce Parameter Object
    files:
      - sync_worker/json_import/process.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"
    exit_signal: JSON 媒体组测试全文件通过
  - id: 2
    title: 统一媒体组纯构造 helper
    method: M-L2-01 Extract Function; M-L2-05 Decompose Conditional
    files:
      - sync_worker/json_import/process.py
      - sync_worker/clone/process.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"; python -m unittest discover -s tests -p "test_sync_services.py"
    exit_signal: JSON 与同步服务测试通过
  - id: 3
    title: 压缩 API 媒体组复制 kwargs 重复构造
    method: M-L2-01 Extract Function; M-L2-05 Decompose Conditional
    files:
      - sync_worker/clone/process.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_sync_services.py"
    exit_signal: 同步服务测试全文件通过
  - id: 4
    title: 运行相关测试集
    method: M-L1-04 Characterization Test
    files:
      - sync_worker/json_import/process.py
      - sync_worker/clone/process.py
      - tests/test_json_sync.py
      - tests/test_sync_services.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"; python -m unittest discover -s tests -p "test_sync_services.py"
    exit_signal: JSON 与同步服务测试文件均通过
checks:
  - step_id: 1
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"
  - step_id: 2
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"; python -m unittest discover -s tests -p "test_sync_services.py"
  - step_id: 3
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_sync_services.py"
  - step_id: 4
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_json_sync.py"; python -m unittest discover -s tests -p "test_sync_services.py"
