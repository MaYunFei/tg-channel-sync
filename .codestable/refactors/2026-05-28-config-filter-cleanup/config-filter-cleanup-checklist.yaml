refactor: 2026-05-28-config-filter-cleanup
status: draft
steps:
  - id: 1
    title: 补配置与过滤刻画测试
    method: M-L1-04 Characterization Test
    files:
      - tests/test_app_config.py
      - tests/test_database.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"; python -m unittest discover -s tests -p "test_database.py" -k "filter"
    exit_signal: 新增配置 truthiness 与过滤规则刻画测试在当前实现下通过
  - id: 2
    title: 提取配置归一化 helper
    method: M-L2-01 Extract Function
    files:
      - app_config.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"
    exit_signal: 配置测试全文件通过
  - id: 3
    title: 拆分消息过滤规则 helper
    method: M-L2-01 Extract Function
    files:
      - database.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_database.py"
    exit_signal: 数据库测试全文件通过
  - id: 4
    title: 运行相关测试集
    method: M-L1-04 Characterization Test
    files:
      - app_config.py
      - database.py
      - tests/test_app_config.py
      - tests/test_database.py
    verification:
      owner: AI
      command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"; python -m unittest discover -s tests -p "test_database.py"
    exit_signal: 配置与数据库测试文件均通过
checks:
  - step_id: 1
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"; python -m unittest discover -s tests -p "test_database.py" -k "filter"
  - step_id: 2
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"
  - step_id: 3
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_database.py"
  - step_id: 4
    type: test
    owner: AI
    command: $env:PYTHONPATH = (Get-Location).Path; python -m unittest discover -s tests -p "test_app_config.py"; python -m unittest discover -s tests -p "test_database.py"
